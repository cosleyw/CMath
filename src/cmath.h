
#ifndef CMATH_CMATH_H
#define CMATH_CMATH_H
#include"common.h"
#include"constants.h"

uint32_t sqrtu32(uint32_t);
float sqrtf32(float);

uint32_t expu32(uint32_t, uint32_t);
float exp2f32(float);

uint32_t log2u32(uint32_t);
uint8_t log2f32(float);

float roundf32(float);
double roundf64(double);

#endif
